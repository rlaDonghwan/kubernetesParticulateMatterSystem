
# MySQL Gyeonggi Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql-gyeonggi
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mysql-gyeonggi
  template:
    metadata:
      labels:
        app: mysql-gyeonggi
    spec:
      containers:
        - name: mysql-gyeonggi
          image: mysql:5.7
          ports:
            - containerPort: 3306
          env:
            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: db-secrets
                  key: MYSQL_ROOT_PASSWORD
            - name: MYSQL_DATABASE
              value: "gyeonggi"
          volumeMounts:
            - name: mysql-gyeonggi-storage
              mountPath: /var/lib/mysql
      volumes:
        - name: mysql-gyeonggi-storage
          persistentVolumeClaim:
            claimName: mysql-gyeonggi-pvc

---
# MySQL Gangwon Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql-gangwon
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mysql-gangwon
  template:
    metadata:
      labels:
        app: mysql-gangwon
    spec:
      containers:
        - name: mysql-gangwon
          image: mysql:5.7
          ports:
            - containerPort: 3307
          env:
            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: db-secrets
                  key: MYSQL_ROOT_PASSWORD
            - name: MYSQL_DATABASE
              value: "gangwon"
          volumeMounts:
            - name: mysql-gangwon-storage
              mountPath: /var/lib/mysql
      volumes:
        - name: mysql-gangwon-storage
          persistentVolumeClaim:
            claimName: mysql-gangwon-pvc

---
# MySQL Chungcheong Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql-chungcheong
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mysql-chungcheong
  template:
    metadata:
      labels:
        app: mysql-chungcheong
    spec:
      containers:
        - name: mysql-chungcheong
          image: mysql:5.7
          ports:
            - containerPort: 3308
          env:
            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: db-secrets
                  key: MYSQL_ROOT_PASSWORD
            - name: MYSQL_DATABASE
              value: "chungcheong"
          volumeMounts:
            - name: mysql-chungcheong-storage
              mountPath: /var/lib/mysql
      volumes:
        - name: mysql-chungcheong-storage
          persistentVolumeClaim:
            claimName: mysql-chungcheong-pvc

---
# MySQL Gyeongsang Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql-gyeongsang
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mysql-gyeongsang
  template:
    metadata:
      labels:
        app: mysql-gyeongsang
    spec:
      containers:
        - name: mysql-gyeongsang
          image: mysql:5.7
          ports:
            - containerPort: 3309
          env:
            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: db-secrets
                  key: MYSQL_ROOT_PASSWORD
            - name: MYSQL_DATABASE
              value: "gyeongsang"
          volumeMounts:
            - name: mysql-gyeongsang-storage
              mountPath: /var/lib/mysql
      volumes:
        - name: mysql-gyeongsang-storage
          persistentVolumeClaim:
            claimName: mysql-gyeongsang-pvc

---
# MySQL Jeolla Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql-jeolla
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mysql-jeolla
  template:
    metadata:
      labels:
        app: mysql-jeolla
    spec:
      containers:
        - name: mysql-jeolla
          image: mysql:5.7
          ports:
            - containerPort: 3310
          env:
            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: db-secrets
                  key: MYSQL_ROOT_PASSWORD
            - name: MYSQL_DATABASE
              value: "jeolla"
          volumeMounts:
            - name: mysql-jeolla-storage
              mountPath: /var/lib/mysql
      volumes:
        - name: mysql-jeolla-storage
          persistentVolumeClaim:
            claimName: mysql-jeolla-pvc

---
# MySQL Jeju Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql-jeju
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mysql-jeju
  template:
    metadata:
      labels:
        app: mysql-jeju
    spec:
      containers:
        - name: mysql-jeju
          image: mysql:5.7
          ports:
            - containerPort: 3311
          env:
            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: db-secrets
                  key: MYSQL_ROOT_PASSWORD
            - name: MYSQL_DATABASE
              value: "jeju"
          volumeMounts:
            - name: mysql-jeju-storage
              mountPath: /var/lib/mysql
      volumes:
        - name: mysql-jeju-storage
          persistentVolumeClaim:
            claimName: mysql-jeju-pvc
